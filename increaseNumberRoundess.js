// Define an integer's roundness as the number of trailing zeroes in it.

// Given an integer n, check if it's possible to increase n's roundness by swapping some pair of its digits.let k = 5;


n = 902200100;

function solution(n) {     
    return /0[1-9]/.test(n)

}

console.log(solution(n))
